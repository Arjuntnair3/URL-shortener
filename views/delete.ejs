<%- include('layout') %>

<form action="/delete/<%= shortURL._id %>" method="POST">
    <input type="text" name="title" class="btn btn-dark" value="<%= shortURL.title %>" required>
    <input type="text" name="full" class="btn btn-dark"value="<%= shortURL.full %>" required>
    <button type="submit" class="btn btn-danger">Delete it</button>
</form>
